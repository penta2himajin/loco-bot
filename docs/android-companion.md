# Android companion notes

See [`android/README.md`](../android/README.md) for the P9 scaffold.

The product brain stays in Rust (`loco-agent`). The Android app is a surface: UI, OS permissions, and process lifecycle around the same turn events (`Clarify`, `ToolRequest`, `Done`, …).
